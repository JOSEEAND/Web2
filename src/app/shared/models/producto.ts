export interface Productos {

    //mismos valores que tiene la entidad Productos
    id: number;
    nombre: string;
    precio: number;
    stock: number;
    fechaIngreso: Date;
    estado: boolean;
}